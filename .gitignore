/target/
*.swp
*.swo
.env
*.db
*.db-shm
*.db-wal
*.sqlite
*.sqlite3

# Web frontend build output & deps
/web/dist/
/web/node_modules/
/web/.svelte-kit/
/web/bun.lock

# Reference projects (separate git repos, not part of rpodder)
/mygpo/
/gpodder2go/
/nextcloud-gpodder/
.env.oauth

# MkDocs build output
/site/
APPUNTI.md

# Helm chart fetched dependencies (recreated by `helm dependency build`)
deploy/**/charts/
deploy/**/Chart.lock

# macOS
.DS_Store
